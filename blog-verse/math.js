export const add = (x, y) => {
    return x + y;
}

export const sub = (x, y) => {
    return x - y;
}

export const mul = (x, y) => {
    return x * y;
}

const div = (x, y) => {
    return x / y;
}

export default div;